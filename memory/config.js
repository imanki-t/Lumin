// Memory folder config — re-exports from master modules/config.js.
// Import memory config from here rather than reaching up to ../../modules/config.js.
export {
  CACHE_ENABLED,
  ENABLE_RAG,
  MEMORY_RECENT_WINDOW,
  MEMORY_MAX_RAG_RESULTS,
  MEMORY_SCORE_THRESHOLD,
  MEMORY_TIME_GAP_MS,
  MEMORY_RAG_CUTOFF_MS,
  MEMORY_MAX_INLINE_CTX,
  MEMORY_CACHE_TTL_MS,
  MEMORY_CACHE_MAX_SIZE,
  MEMORY_CACHE_MIN_QUERY_LEN,
  MEMORY_CACHE_SEMANTIC_SIM,
  MEMORY_CHUNK_SIZE,
  MEMORY_CHUNK_OVERLAP,
  MEMORY_INDEX_BATCH_SIZE,
  MEMORY_PERSONAL_CACHE_TTL_MS,
  CLUSTER_MAX,
  CLUSTER_NUM_BASELINE,
  CLUSTER_MIN_MEMORIES,
  CLUSTER_TOP_TO_SEARCH,
  CLUSTER_MIN_SIMILARITY,
  CLUSTER_REINDEX_INTERVAL,
  CLUSTER_MAX_KMEANS_ITERS,
  CLUSTER_CONVERGENCE_THRESHOLD,
  CLUSTER_CACHE_TTL_MS,
  CLUSTER_MAX_PER_CLUSTER,
  CLUSTER_EMBEDDINGS_TTL_MS,
  CLUSTER_EMBEDDING_LIMITS,
  EMBEDDING_MODEL,
  EMBEDDING_CACHE_MAX_SIZE,
  EMBEDDING_MAX_CONCURRENT,
  EMBEDDING_DIM,
  EMBEDDING_MRL_SHORT_DIM,
  EMBEDDING_MRL_CENTROID_DIM,
  EMBEDDING_REDIS_TTL,
  EMBEDDING_REDIS_PREFIX,
  EMBEDDING_ENABLE_PDF,
  EMBEDDING_ENABLE_VIDEO,
  EMBEDDING_ENABLE_AUDIO,
  EMBEDDING_IMAGE_LIMIT,
  EMBEDDING_PDF_LIMIT,
  EMBEDDING_VIDEO_LIMIT,
  EMBEDDING_AUDIO_LIMIT
} from '../modules/config.js';
